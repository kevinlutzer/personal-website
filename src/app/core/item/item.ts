export interface Item {
    title: string;
    subTitle: string;
    description: string;
    startDate?: string;
    endDate?: string;
    imageUrl?: string;
    iconUrl?: string;
    actionUrl?: string;
    tags?: string[];
}
