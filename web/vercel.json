{
  "version": 2,
  "builds": [
    {
      "src": "web/",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}